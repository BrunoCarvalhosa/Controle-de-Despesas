import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AtualizadespesaPage } from './atualizadespesa.page';

describe('AtualizadespesaPage', () => {
  let component: AtualizadespesaPage;
  let fixture: ComponentFixture<AtualizadespesaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AtualizadespesaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
