import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AtualizareceitaPage } from './atualizareceita.page';

describe('AtualizareceitaPage', () => {
  let component: AtualizareceitaPage;
  let fixture: ComponentFixture<AtualizareceitaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AtualizareceitaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
